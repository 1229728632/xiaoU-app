.navBox {
    display: flex;
    width: 100vw;
    height: 0.9rem;
     justify-content: space-between

    .navItem {
        display: flex;
        width: 0.7rem;
        height: 0.7rem;
        flex-direction: column;
        align-items: center;

        img {
            width: 0.7rem;
            height: 0.7rem;
        }
    }
}